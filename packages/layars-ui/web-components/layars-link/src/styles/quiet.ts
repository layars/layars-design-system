import { css } from 'lit'

const quiet = css`
    .quiet {
        text-decoration: none;

        &:hover {
            text-decoration: underline;
        }
    }
`

export { quiet }

export default quiet